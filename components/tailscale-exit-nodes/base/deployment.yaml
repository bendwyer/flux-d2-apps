---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: tailscale-exit-nodes
  namespace: tailscale-exit-nodes
  labels:
    app: tailscale-exit-nodes
spec:
  replicas: 1
  strategy:
    type: Recreate
  selector:
    matchLabels:
      app: tailscale-exit-nodes
  template:
    metadata:
      labels:
        app: tailscale-exit-nodes
    spec:
      containers:
        - name: tailscale-exit-nodes
          image: ghcr.io/bendwyer/containers/tailscale-exit-nodes:1.2.1@sha256:3f188bcad03dcfef2f6429b97dc3fa3367cfd4b7f1aab414171398e590e240eb
          ports:
            - containerPort: 8080
          env:
            - name: DATABASE_URL
              valueFrom:
                secretKeyRef:
                  name: tailscale-exit-nodes-db-app
                  key: uri
            - name: AWS_ACCESS_KEY_ID
              valueFrom:
                secretKeyRef:
                  name: exit-nodes-aws
                  key: username
            - name: AWS_SECRET_ACCESS_KEY
              valueFrom:
                secretKeyRef:
                  name: exit-nodes-aws
                  key: credential
            - name: VULTR_API_KEY
              valueFrom:
                secretKeyRef:
                  name: exit-nodes-vultr
                  key: credential
            - name: TAILSCALE_OAUTH_CLIENT_ID
              valueFrom:
                secretKeyRef:
                  name: exit-nodes-tailscale
                  key: username
            - name: TAILSCALE_OAUTH_CLIENT_SECRET
              valueFrom:
                secretKeyRef:
                  name: exit-nodes-tailscale
                  key: credential
          livenessProbe:
            httpGet:
              path: /healthz
              port: 8080
            initialDelaySeconds: 5
            periodSeconds: 30
          readinessProbe:
            httpGet:
              path: /readyz
              port: 8080
            initialDelaySeconds: 5
            periodSeconds: 10
          resources:
            requests:
              cpu: 10m
              memory: 128Mi
            limits:
              memory: 128Mi
